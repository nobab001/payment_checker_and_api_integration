import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // App requires Firebase init — integration tests go in integration_test/
    expect(true, isTrue);
  });
}
